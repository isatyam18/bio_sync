"use client"
import {useState} from "react";import Link from "next/link";import {Activity,ArrowRight,Eye,EyeOff} from "lucide-react";import {useRouter} from "next/navigation";import {api} from "@/lib/api"
export function AuthForm({mode}:{mode:"signup"|"login"}){const signup=mode==="signup";const [show,setShow]=useState(false);const [loading,setLoading]=useState(false);const [error,setError]=useState("");const router=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setError("");setLoading(true);const fd=new FormData(e.currentTarget);try{await api(signup?"/auth/signup":"/auth/login",{method:"POST",body:JSON.stringify(Object.fromEntries(fd.entries()))});router.push("/dashboard")}catch(e){setError(e instanceof Error?e.message:"Something went wrong")}finally{setLoading(false)}}return <div className="flex min-h-screen flex-col bg-background"><header className="mx-auto flex w-full max-w-6xl items-center justify-between px-5 py-5 sm:px-8"><Link href="/" className="flex items-center gap-2.5"><span className="flex h-8 w-8 items-center justify-center rounded-lg bg-primary text-primary-foreground"><Activity className="h-4 w-4"/></span><span className="text-lg font-semibold tracking-tight">BioSync</span></Link><Link href="/" className="text-sm text-muted-foreground">Back to home</Link></header><main className="flex flex-1 items-center justify-center px-5 py-10"><div className="w-full max-w-md"><div className="rounded-2xl border border-border bg-card p-8 shadow-sm"><span className="text-xs font-semibold uppercase tracking-[0.18em] text-primary">{signup?"Create your account":"Welcome back"}</span><h1 className="mt-3 font-serif text-3xl font-semibold tracking-tight">{signup?"Get started with BioSync":"Log in to BioSync"}</h1><p className="mt-2 text-sm leading-relaxed text-muted-foreground">{signup?"Create a workspace for model-based health risk assessments.":"Access your patients and saved assessments."}</p>{error&&<div className="mt-5 rounded-xl border border-destructive/20 bg-destructive/5 p-3 text-sm text-destructive">{error}</div>}<form onSubmit={submit} className="mt-6 space-y-4">{signup&&<Field label="Full name"><input name="name" required className="input" placeholder="Your name"/></Field>}<Field label="Email"><input name="email" type="email" required className="input" placeholder="you@example.com"/></Field><Field label="Password"><div className="relative"><input name="password" type={show?"text":"password"} minLength={8} required className="input pr-11" placeholder="At least 8 characters"/><button type="button" onClick={()=>setShow(!show)} className="absolute right-3 top-1/2 -translate-y-1/2 text-muted-foreground">{show?<EyeOff className="h-4 w-4"/>:<Eye className="h-4 w-4"/>}</button></div></Field>{signup&&<Field label="Account type"><select name="role" required defaultValue="patient" className="input"><option value="patient">Patient</option><option value="doctor">Doctor</option></select></Field>}<button disabled={loading} className="mt-2 inline-flex w-full items-center justify-center gap-2 rounded-full bg-primary px-6 py-3 text-sm font-semibold text-primary-foreground disabled:opacity-60">{loading?"Please wait…":signup?"Create account":"Log in"}<ArrowRight className="h-4 w-4"/></button></form></div><p className="mt-6 text-center text-sm text-muted-foreground">{signup?"Already have an account? ":"New to BioSync? "}<Link href={signup?"/login":"/signup"} className="font-semibold text-primary">{signup?"Log in":"Create an account"}</Link></p></div></main></div>}
function Field({label,children}:{label:string;children:React.ReactNode}){return <div><label className="mb-1.5 block text-sm font-medium">{label}</label>{children}</div>}

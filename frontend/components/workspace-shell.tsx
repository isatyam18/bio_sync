"use client";import Link from "next/link";import {Activity,LogOut} from "lucide-react";import {useRouter} from "next/navigation";import {api} from "@/lib/api";import {useEffect,useState} from "react";export function Shell(){const r=useRouter();const [role,setRole]=useState<string>();useEffect(()=>{api<any>('/auth/me').then(x=>setRole(x.user?.role))},[]);return <header className="border-b bg-background/90"><div className="mx-auto flex h-16 max-w-6xl items-center justify-between px-5"><Link href="/dashboard" className="flex items-center gap-2 font-semibold"><span className="flex h-8 w-8 items-center justify-center rounded-lg bg-primary text-primary-foreground"><Activity className="h-4 w-4"/></span>BioSync</Link><nav className="flex items-center gap-5 text-sm"><Link href="/dashboard">Dashboard</Link>{role==='doctor'&&<Link href="/patients">Patients</Link>}<button onClick={async()=>{await api('/auth/logout',{method:'POST'});r.push('/')}}><LogOut className="h-4 w-4"/></button></nav></div></header>}
